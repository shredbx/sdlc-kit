# A path inside a folder: no leading /, no . or .. step. apps/hello-app, dist/a-0.1.0.tar.gz.
name: relative-path
type: string
pattern: '(?!\.\.?(/|$))[A-Za-z0-9_.-]+(/(?!\.\.?(/|$))[A-Za-z0-9_.-]+)*'
