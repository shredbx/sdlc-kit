---
name: {{ skill }}
description: {{ description }}
---

# {{ skill }}

{{ description }}

Nothing yet beyond this. The skill was made from the template `process-os.claude-plugin`, and the
plugin's own task adds the real judgment — not what the plugin's tools do, each already says that
itself, but when to reach for which one.
