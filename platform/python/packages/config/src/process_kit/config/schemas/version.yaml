# The shape of processos.yaml itself: major.minor.patch, changed only when process-cli's own config
# format changes. Optional: a config written before this field existed has none.
name: version
type: string
pattern: '[0-9]+\.[0-9]+\.[0-9]+'
