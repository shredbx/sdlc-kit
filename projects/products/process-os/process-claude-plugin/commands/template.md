---
description: A template request for process-cli — render a template's files, or check a folder still conforms — routed to using-process-os.
argument-hint: <what you want to do, e.g. "render sdlc.python.application into apps/hello-app">
---

# /template

$ARGUMENTS

This is a `template` request. Use the skill `using-process-os`: read its own `tools/template.md`
(`render`, `conform`) for the exact command line, what it does and what it prints, then act.
