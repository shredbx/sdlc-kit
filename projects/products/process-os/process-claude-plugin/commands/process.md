---
description: A process or action request for process-cli — run one, resume a stopped run, or read past runs — routed to using-process-os.
argument-hint: <what you want to do, e.g. "run sdlc.build-application for hello-app">
---

# /process

$ARGUMENTS

This is a `process` request. Use the skill `using-process-os`: read its own `tools/process.md`
(`run`, `resume`, `runs`) for the exact command line, what it does and what it prints, then act —
and see the skill's own "Read a stopped run before retrying blind" and "Prefer a process's own
graph over its steps by hand" before calling anything.
