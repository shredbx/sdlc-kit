---
description: A schema request for process-cli — validate a data file, or create a new instance — routed to using-process-os.
argument-hint: <what you want to do, e.g. "validate hello-app/application.yaml against sdlc.application">
---

# /schema

$ARGUMENTS

This is a `schema` request. Use the skill `using-process-os`: read its own `tools/schema.md`
(`validate`, `create`, `edit`) for the exact command line, what it does and what it prints, then act.
