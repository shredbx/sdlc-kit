# docs/tasks/_done/260921-process-cli-01/steps/m3-8-cli-commands.md §3 — process-cli validate: a message with no path, exact
- id: V4
  files: {processos.yaml: "definitions: defs\nruntime:\n  root: .\n"}
  folders: [defs]
  args: [validate, sdlc.greeting, nofile.yaml]
  expect: {stderr: ["nofile.yaml: no such file"]}
